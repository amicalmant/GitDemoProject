mkdir folder
ls folder
git init
git status
touch file
git status
git config --global user.email "you@example.com"
git config --global user.name "Your Name"

git add .
git commit -m "message"
(ou git commit -a -m "message")

git log --oneline
git diff

See the rest here: https://goo.gl/oS4ZbG

git push origin master
git pull
